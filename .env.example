# Demo switch (client-side).
# true  = deterministic local mock outputs, no Gemini/API key required
# false = call /api/generate and use server-side Gemini
VITE_USE_MOCK=true

# Optional app URL
VITE_APP_URL=

# Server-side only (used by /api/generate in Vercel)
GEMINI_API_KEY=
